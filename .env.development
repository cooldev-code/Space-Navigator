# Browser calls this path on the dev server (same origin → no CORS). Vite proxies it to API_PROXY_TARGET.
VITE_API_BASE_URL=/api/v1

# Dev-only, server-side: Vite proxy target for /api/v1 (no VITE_ prefix — not sent to the client bundle).
API_PROXY_TARGET=https://space-navigator-pi.vercel.app