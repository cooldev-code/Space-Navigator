VITE_API_BASE_URL=/api/v1

# Dev-only: where Vite proxies /api/v1 (see vite.config.ts). Omit or use http://127.0.0.1:5050 for the local server.
VITE_DEV_API_PROXY_TARGET=https://space-navigator-gold.vercel.app
